const fs = require('fs');
const rl = require('readline');
require('require');
// console.log(req);

// const data = rl.createInterface({
//     input: fs.createReadStream('./multiline.txt')
// })

// data.on('line', (line) => console.log(line));
// data.on('close', () => console.log("Done and closed"))